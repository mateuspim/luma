# luma

A TUI brightness controller for DDC/CI monitors using `ddcutil`.

## Requirements

- `ddcutil` installed and accessible
- DDC/CI enabled on your monitors

## Installation

```sh
go install github.com/pym/luma@latest
```

## Usage

```sh
luma
```
